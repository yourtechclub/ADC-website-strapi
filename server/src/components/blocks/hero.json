{
  "collectionName": "components_blocks_heroes",
  "info": {
    "displayName": "Hero",
    "description": "Hero section with heading, optional subtitle, menu items shown below title on desktop, and CTA buttons"
  },
  "options": {},
  "attributes": {
    "heading": {
      "type": "string",
      "required": true
    },
    "subtitle": {
      "type": "text"
    },
    "showMenuItemsInHero": {
      "type": "boolean",
      "default": true,
      "description": "Show navigation menu items below heading (desktop only)"
    },
    "ctaButtons": {
      "type": "component",
      "repeatable": true,
      "component": "shared.link"
    },
    "backgroundImage": {
      "allowedTypes": [
        "images"
      ],
      "type": "media",
      "multiple": false
    }
  }
}
